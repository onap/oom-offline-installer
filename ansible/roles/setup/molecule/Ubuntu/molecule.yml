---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: instance
    image: ${PLATFORM_DISTRO:-ubuntu}:${DISTRO_VERSION:-18.04}
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: ubuntu
verifier:
  name: testinfra
  lint:
    name: flake8
