#
# Define environment variables here that are visible for each Molecule run.
# Format is shell format.
#
# Alternative, env variables can be also defined specificly for certain role
# by creating .env.yml file (yml format) into role's directory from where also
# Molecule is run. Variables in this role specific .env.yml file will override
# variables in this file.
#

# Passwords needed (when using molecule-dev container) to access localhost by ssh
#LOCALHOST_ANSIBLE_PASSWORD=
#LOCALHOST_ANSIBLE_SUDO_PASS=

